use_default_settings: true

general:
  instance_name: "FenrixChat Search"

search:
  formats:
    - html
    - json

server:
  secret_key: "fenrixchat-searxng-secret-key"
  bind_address: "0.0.0.0"
  port: 8080
  public_instance: false
  limiter: false

